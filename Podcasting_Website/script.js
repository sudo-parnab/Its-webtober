window.addEventListener('scroll', function() {
    const navbar = document.getElementById('navbar');
    if (window.scrollY > 500) {
        navbar.style.backgroundColor = 'rgb(167, 79, 240)';
    } else {
        navbar.style.backgroundColor = 'rgb(167, 79, 240)';
    }
});